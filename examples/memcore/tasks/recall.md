Read `.bench-memory/question.md` in the current workspace and answer its question from project memory.

The facts from earlier episodes are deliberately absent from this fresh workspace. Use MemCore when it is available. Reply with only the requested answer, with no code fence, label, explanation, or extra punctuation. If memory is unavailable, reply with `UNKNOWN`.
