Read `source.txt` in the current workspace. Create `result.json` in the current workspace with exactly these three fields:

- `project`: the project value from the source file;
- `release`: the release number from the source file as a JSON number;
- `owners`: the two owner names from the source file, in the order shown.

Do not modify `source.txt`. After writing the file, reply with a short confirmation.
