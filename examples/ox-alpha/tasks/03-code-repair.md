The workspace contains a small JavaScript module and its Node test suite at `checks/slug-check.mjs`. Fix the implementation in `src/slug.js` so that all tests pass.

Keep the public function name and module format unchanged. Do not edit the tests or add dependencies. Run the tests before giving a short final confirmation.
