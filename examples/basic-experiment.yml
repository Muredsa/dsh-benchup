version: 1
runs: 1
schedule: paired-shuffled

models:
  default:
    provider: deepseek-official
    model: deepseek-v4-flash
    temperature: 0

variants:
  baseline:
    profile: headless

scenarios:
  - id: basic/reply
    fixture: fixture
    task: basic-task.md
    evaluators:
      type: exact
      expected: expected.txt
